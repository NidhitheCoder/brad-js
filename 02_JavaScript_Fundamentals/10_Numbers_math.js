const num1 = 150;
const num2 = 100;
let val;

// basic maths
val = num1 + num2;
val =  num1 - num2;
val = num1 * num2;
val = num1 / num2;
val = num1 % num2;

// math objects
val = Math.PI;
val = Math.E;
val = Math.round(2.8); //output 3
val = Math.round(2.4); // output 2

val = Math.ceil(2.4);
val = Math.floor(2.8);
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.pow(2,4);
val = Math.min(2,33,43,53,1,53,55);
val = Math.max(23,43,54,65,76,84,53);
val = Math.random();
val = Math.random() * 20;

val = Math.floor(Math.random() * 20);
console.log(val);